# Dependencies — reinstalled from package.json, never committed
node_modules/

# Compiled output — generated from src/ by `tsc`
dist/

# Secrets & environment config — NEVER commit this
.env
.env.render
admin-credentials.local.txt

# macOS junk
.DS_Store
.neon
